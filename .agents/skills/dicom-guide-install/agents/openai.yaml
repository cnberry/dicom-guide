interface:
  display_name: "Install DICOM Guide"
  short_description: "Bootstrap and open a local medical scan folder"
  default_prompt: "Use $dicom-guide-install to fetch DICOM Guide if needed, install it, open my scan folder, and begin a guided local session."
